import { ReactFlowProvider } from 'reactflow';
import FactoryMethod from './FactoryMethod';

function App() {
  return (
  <ReactFlowProvider>
    <FactoryMethod />
  </ReactFlowProvider>    
    );
}

export default App;
